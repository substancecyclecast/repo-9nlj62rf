Тема: [SnabAgent #LOT-{{ lot_short_id }}] Запрос коммерческого предложения: {{ category_human }}

Уважаемые коллеги!

ООО "{{ customer_name }}" формирует пакет коммерческих предложений для дальнейшей закупки. Прошу направить ваше КП на следующую номенклатуру:

{% for item in items %}{{ loop.index }}. {{ item.matched_nsi_name or item.raw_phrase }}
   Количество: {{ item.qty }} {{ item.unit }}
   Срок поставки: {{ item.lead_time_days or 'по предложению поставщика' }} дней
   {% if item.gost %}ГОСТ: {{ item.gost }}{% endif %}
{% endfor %}

Формат ответа:
- цена за единицу и итоговая сумма с НДС,
- срок поставки,
- условия оплаты,
- срок действия предложения.

Срок ответа: {{ rfq_deadline }} (МСК).

Ответ направить на адрес: {{ reply_to_email }} (сохраняйте в теме ID #LOT-{{ lot_short_id }} для автоматической маршрутизации).

С уважением,
Команда снабжения "{{ customer_name }}"
(письмо сформировано автоматически системой SnabAgent — авторизованным агентом отдела снабжения для сбора ТКП на этапе формирования НМЦК)
